# ArcSight Rule Generator

Generates ArcSight correlation rules from templates.

## Usage

1. Update templates in `src/templates.py`
2. Run `python src/rules.py`
3. New rules will be created in `output/` folder